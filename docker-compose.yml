version: '3.8'

services:
  scraper:
    build: .
    ports:
      - "3006:8501"
    volumes:
      - ./filings:/app/filings
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
